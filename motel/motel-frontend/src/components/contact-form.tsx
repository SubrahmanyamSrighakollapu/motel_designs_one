"use client";
import {useState} from 'react';
import {api} from '@/lib/api';
export default function ContactForm(){const[status,setStatus]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;setBusy(true);setStatus('');try{await api('/contact',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});setStatus('Thank you. Your message has been received.');form.reset();}catch(e){setStatus((e as Error).message);}finally{setBusy(false);}}return <form onSubmit={submit} className="form-panel"><h2>Drop us a line.</h2><div className="form-grid"><label>Your name<input name="name" autoComplete="name" required minLength={2} maxLength={100}/></label><label>Email address<input name="email" type="email" autoComplete="email" required maxLength={254}/></label></div><label>Subject<select name="subject"><option>General enquiry</option><option>My stay</option><option>Pet-friendly rooms</option><option>Accessibility</option></select></label><label>Your message<textarea name="message" rows={5} required minLength={10} maxLength={4000}/></label><p className="fine">We use your details to respond to this enquiry. <a className="underline" href="/privacy">Privacy policy</a></p><button className="button" disabled={busy}>{busy?'Sending…':'Send message'}</button><p role="status">{status}</p></form>}
